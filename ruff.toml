# Support Python 3.10+.
target-version = "py310"
# Set the maximum line length to 110 characters.
line-length = 110

[lint]
# Add the `line-too-long` rule to the enforced rule set.
extend-select = [
    "E501",
    "ANN",
    "ASYNC",
    "TC",
    "N",
    "S",
    "I",
]
